totalWinnings = 0
get the number of times spinning wheel
for each spin
    get guess
    print wheel spin
    get reroll confirm
        if reroll = yes: spin wheel
        otherwise: nothing
    determine win or lose
        if abs(guess - wheel spin) == 25: determine amount won
            if reroll.lower() = n, no:
                if 2500 > 50 * wheelSpin: print 2500
                otherwise: print 50* wheelSpin:
            otherwise: print wheelSpin * 20
        totalWinnings += winnings
        otherwise: print you lose
print totalWinnings
